# En Vercel configura estas variables en Project Settings > Environment Variables:
#   DATABASE_URL      (cadena de Neon)
#   JWT_SECRET        (un secreto largo y aleatorio, NO el de ejemplo)
#   VAPID_PUBLIC_KEY  (notificaciones push; generar con: npx web-push generate-vapid-keys)
#   VAPID_PRIVATE_KEY (notificaciones push; mantener secreta)
#   VAPID_SUBJECT     (mailto:tu-correo)
#   DEV_EMAIL         (correo del desarrollador; unica cuenta que puede publicar novedades)
# APP_ORIGIN y PORT solo aplican para desarrollo local.
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
JWT_SECRET=change-this-secret
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:tu-correo@ejemplo.com
DEV_EMAIL=
APP_ORIGIN=http://localhost:5173
PORT=8787
